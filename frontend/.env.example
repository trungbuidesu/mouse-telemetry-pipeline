# Frontend local environment example
# Copy to frontend/.env for local overrides. Do not commit real secrets.

# Base URL for the ingestion API (used by future telemetry sender)
VITE_API_BASE_URL=http://127.0.0.1:8001

# Optional display flags for local development
VITE_APP_ENV=local
